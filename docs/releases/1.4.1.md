# 1.4.1 production release — verified 2026-09-09

One integration release, PR #406, contains the complete intended #404 and #405 changes plus the required release blockers. Neither source PR was merged independently. Release base: `a33d9205db9cfc3f59b1837d8f1a86884c94524d` (1.4.0).

- Production source: `1f2a6563c6d2a3fdfe5b6170b9a747714e89aade`.
- Product version: **1.4.1**.
- Release ID: `gclr-ba1f29e6bc17552c3e41c9213e187a7047ca227ad4030185940f690455b8f664`.
- `instructions_sha256`: `2f371af4f45dca097654bbdc9cd83890530d255a33a9b10f10d838b2738d732d`.
- `tool_catalogue_sha256`: `dd68b0b93aad382d8136fecd336c7977b739526ef404a3acba2c0ceb4eedcd42`.
- `skill_sha256`: `fa8342cf49f9b47f137f223f318d205bc65200798994ab4f25ba3bf52f978c17`.
- `gateway_artifact_sha256`: `252df0b4642ff96d77c48321061ea28c90120f4b5e3e308125c84f17a953cf91`.

All values were rebuilt from the exact merged source, then verified against canonical production `/readyz`. `release_bundle.py verify` also matched the deployment identity independently derived from the existing production configuration. Railway deployment `c857dba7-6e6c-4472-ace4-d1dc9458dc15` serves this release.

**Registry:** [hardened OIDC workflow 34360874543](https://github.com/atomchung/long-run-hybrid-coach/actions/runs/34360874543) passed after production identity verification. Direct versioned Registry read-back (`io.github.atomchung/long-run-hybrid-coach`, version `1.4.1`) reports 1.4.1, the canonical remote, `active`, `isLatest=true`, published `2026-09-09T14:01:48.306164Z`. The publication gate was also exercised before promotion and refused the stale production source. #283 is implemented and exercised, not merely configured.

**Validation:** combined local suite 2,659 tests, one platform/environment skip; full [integration CI](https://github.com/atomchung/long-run-hybrid-coach/actions/runs/34360097020) and [production CI](https://github.com/atomchung/long-run-hybrid-coach/actions/runs/34360416903) passed. Repository safety passed; 52 explicit context/view/journey/orchestration budget tests passed. Independent combined OAuth/model-surface adversarial review and re-review found no remaining release blocker. An initial CI failure caught Python 3.11.16's changed mapped-IPv6 formatting; the final serializer passed 10,264 independent Node WHATWG comparisons with zero mismatches.

**Live protocol receipts:** ChatGPT, claude.ai and claude.com callbacks register and reach Intervals directly, with no Coach consent/cookie. An unknown HTTPS callback registers and receives the Coach page. Cancel returns no provider redirect or Coach code/token. A synthetic client answering its own page receives a fresh-browser callback refusal before provider-code exchange. Six alternate authority registrations were refused. Existing formal Codex connector `getCoachState` succeeded against production.

**Client acceptance is separate:** ChatGPT and Claude connector refresh actions were performed after deployment. ChatGPT's refreshed schema visibly contains the new explicit-denial `all_clear` text. A fresh native ChatGPT conversation completed `startCoachSession` and connection inspection successfully, received the new coaching guidance, omitted `all_clear`, distinguished unmatched activity from confirmed zero training, and discussed the cycle adjustment/stop conditions with unknowns preserved. No new workout was written solely for this release. Claude loaded tools but hit its account usage limit before completing the read. The complete previously-unverified hosted HTTPS client's real-provider positive journey remains pending: automatic approval review rejected a temporary Cloudflare tunnel carrying the callback code without explicit approval for that egress path. Cancel/self-consent refusal is verified live; the full positive path is verified by HTTP regressions with a fake provider, not yet a real-provider hosted-client receipt.

**Scope:** no #352, #399, #395, #397 or #312 work was added. 1.4.1 removes operator-origin admission friction for currently supported legacy MCP revisions; it does not claim universal MCP 2026-07-28 support. Gemini/OpenClaw five-step acceptance was not rerun. OpenAI resubmission/review/approval is not performed by this release; the changed digests require a new reviewed snapshot and must not be represented as the earlier submission.

**Scheduled residual:** structural aggregation now includes the reproduced request/session values and goal/week/cycle siblings. Some nested subobjects (measurement/outlook/fallback) still return their first internal error. The trigger for further work is a normal client again requiring avoidable repair round trips inside those objects; this release does not claim exhaustive recursive error aggregation.
