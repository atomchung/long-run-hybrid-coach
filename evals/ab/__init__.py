"""One coaching question asked of three context builds, so a change can be compared.

See ``README.md`` beside this file. The package is deliberately importable without any
of the product's runtime being touched: everything here reads fixtures and writes to a
run store outside the repository.
"""
